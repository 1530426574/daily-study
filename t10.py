def recursion(self, level, param1, param2, *args):
    # recursion terminator
    if level > MAX_LEVE:
        proces_result
        return

    # process logic in current level
    process(level, args)

    # drill down
    self.recursion(level + 1, p1, ...)

    # reverse the current level status if needed
